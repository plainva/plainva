# Sneltoetsen

Laatst bijgewerkt: 2026-07-15

Druk op `F1` (of **Opdrachtenpalet → Sneltoetsen tonen**) om op elk moment het venster met sneltoetsen te openen. Het groepeert elke sneltoets en muisgebaar in gebieden waartussen je bovenaan met de chips wisselt, heeft een zoekveld dat over alle gebieden werkt, en herkent je platform automatisch: het toont `Ctrl`/`Alt` onder Windows en Linux en `⌘`/`⌥` onder macOS. In de tabellen hieronder betekent `Ctrl` daarom `⌘` op macOS en `Alt` betekent `⌥`.

## Algemeen

| Sneltoets | Actie |
|---|---|
| `Ctrl+P` (of `Ctrl+Shift+P`) | Opdrachtenpalet openen |
| `Ctrl+O` | Snelkiezer – bestanden zoeken en openen |
| `Ctrl+,` | Instellingen openen |
| `Ctrl+Plus` / `Ctrl+Minus` | Interface in- / uitzoomen (80–150 %) |
| `Ctrl+0` | Zoom terugzetten naar 100 % |
| `Ctrl+S` | Opslaan bevestigen (Plainva slaat automatisch op) |
| `F1` | Dit venster tonen |
| `Esc` | Actie annuleren (dialoog sluiten, hernoemen annuleren) |

## Notities & bestanden

| Sneltoets | Actie |
|---|---|
| `Ctrl+N` | Nieuwe notitie in de geselecteerde map |
| `Ctrl+Shift+D` | Dagnotitie van vandaag openen |
| `F2` | De actieve notitie hernoemen |
| `Ctrl+Alt+Left` / `Ctrl+Alt+Right` | Terug / vooruit (navigatiegeschiedenis) |

In de bestandsboom: **klik** selecteert en opent, `Ctrl`+klik schakelt de multiselectie, `Shift`+klik selecteert een bereik, **middelklik** opent een bestand in een nieuw tabblad, **slepen** verplaatst bestanden, **rechtsklik** opent het contextmenu en `Delete` verplaatst de selectie naar de prullenbak.

## Weergave, vensters & tabbladen

| Sneltoets | Actie |
|---|---|
| `Ctrl+E` | Lezen ↔ bewerken wisselen |
| `Ctrl+Shift+E` | Live-voorbeeld ↔ Markdown-bron wisselen |
| `Ctrl+Alt+B` / `Ctrl+Alt+R` | Linker- / rechterzijbalk tonen/verbergen |
| `Ctrl+Alt+V` / `Ctrl+Alt+S` | Editor splitsen – naast elkaar / boven elkaar |
| `Ctrl+Shift+G` | Graaf / vault-kaart openen |
| `Ctrl+Shift+F` | Zoeken en vervangen in de vault |
| `Ctrl+T` | Nieuw tabblad (notitie kiezen om te openen) |
| `Ctrl+W` | Tabblad sluiten |
| `Ctrl+Shift+T` | Het laatst gesloten tabblad opnieuw openen |
| `Ctrl+Tab` / `Ctrl+Shift+Tab` | Volgend / vorig tabblad (`Ctrl` op elk besturingssysteem) |
| `Ctrl+1` … `Ctrl+8` | Naar tabblad 1–8 springen |
| `Ctrl+9` | Naar het laatste tabblad springen |

De focusmodus (beide zijbalken verborgen) vind je in het opdrachtenpalet.

## Tekst opmaken

Werkt in de editor (live-voorbeeld of Markdown-bron):

| Sneltoets | Actie |
|---|---|
| `Ctrl+B` / `Ctrl+I` | Vet / cursief |
| `Ctrl+Shift+S` | Doorhalen |
| `Ctrl+Shift+H` | Markering (`==markering==`) |
| `Ctrl+K` | Link invoegen / selectie omzetten in link |
| `Ctrl+Shift+1` / `2` / `3` | Kop 1 / 2 / 3 |
| `Ctrl+Shift+0` | Normale tekst (kop verwijderen) |
| `Ctrl+Enter` | Selectievakje van de taak wisselen |

Je kunt ook tekst selecteren voor de zwevende werkbalk (vet, cursief, doorhalen, code, markering, link), en een gerenderd `[ ]`-selectievakje aanklikken om een taak in elke modus te wisselen.

## Tekst bewerken

| Sneltoets | Actie |
|---|---|
| `Ctrl+Z` / `Ctrl+Y` (of `Ctrl+Shift+Z`) | Ongedaan maken / opnieuw |
| `Ctrl+A` | Alles selecteren |
| `Tab` / `Shift+Tab` | Inspringen / uitspringen |
| `Alt+Up` / `Alt+Down` | Regel omhoog / omlaag verplaatsen |
| `Shift+Alt+Down` | Regel dupliceren |
| `Ctrl+Shift+K` | Regel verwijderen |
| `Ctrl+D` | Volgende overeenkomst aan de selectie toevoegen |
| `Ctrl+F` | Zoeken en vervangen in de notitie |
| `Ctrl+G` / `F3` | Volgende zoeken (Shift voor vorige) |
| `Ctrl+Alt+G` | Naar regel gaan |
| `Ctrl+Shift+[` / `Ctrl+Shift+]` | Invouwen / uitvouwen (macOS: `⌘⌥[` / `⌘⌥]`) |
| `Ctrl+Space` | Automatisch aanvullen activeren |

## Invoegen

Typ dit in de editor:

| Invoer | Actie |
|---|---|
| `/` | Opdrachtmenu (blokken, opmaak, sjablonen, emoji) |
| `@` | Vermelding: datum, notitielink of database |
| `[[` | Wiki-link – notitie kiezen |
| `![[` | Notitie of afbeelding insluiten |
| `#` | Een tag aanvullen |
| `:name` | Emoji via naam (voegt het teken in) |
| `Ctrl+Alt+T` | Sjabloon invoegen |

Een bestand in de editor neerzetten sluit een afbeelding in (of kopieert een ander bestand naar de vault en koppelt het).

## Graaf

| Sneltoets | Actie |
|---|---|
| `Ctrl+Shift+G` | Graaf / vault-kaart openen |
| Pijltoetsen | Focus tussen knopen verplaatsen |
| `Enter` | De gefocuste knoop openen |

Muis: **scrollen** om te zoomen; **middelste muisknop + slepen** of `Ctrl`+slepen om te verschuiven (ook over een knoop); **slepen op een lege plek** spant een selectiekader op (vault-kaart); `Alt`+slepen verplaatst een knoop met zijn gekoppelde buurknopen (vault-kaart); **een knoop op een andere knoop slepen** maakt een koppeling of databaserelatie (vault-kaart); een enkele klik opent (context-/databasegraaf) of selecteert (vault-kaart); **dubbelklik op een mapbel** om hem uit te vouwen (dubbelklik op de rand om in te vouwen); `Ctrl`+klik opent in de split, **middelklik** opent in een nieuw tabblad; **rechtsklik** voor het contextmenu; de **pin-naald** (rechtsboven) schakelt of posities worden onthouden.

## Databases (.base)

| Invoer | Actie |
|---|---|
| Klik op een cel | Direct bewerken; een selectievakje schakelt direct |
| Klik op een kaart / rij | De notitie in een peek-venster openen |
| `Ctrl`+klik op een kaart | De notitie in de split openen |
| Kaart slepen | Bord: kolomwaarde instellen; kalender/tijdlijn: datum instellen |
| Kolomkop slepen | Kolommen verplaatsen (en de opties van de eigenschap) |
| Klik op een tabelkop | Sorteervolgorde wijzigen |
| Klik / sleep de weergavepil | Van weergave wisselen; slepen om te verplaatsen; `▾` = hernoemen/dupliceren/verwijderen |
| **+ Item** | Nieuw item met de standaardsjabloon (`▾` = sjabloon/map) |

## Muis & gebaren (overal)

- **Blokgreep (`⠿`):** aantikken voor het blokmenu (omzetten in, dupliceren, verplaatsen, verwijderen); slepen om te verplaatsen.
- **Tabbladen:** slepen om te herordenen, tussen panelen, of naar een rand om te splitsen.
- **Links:** klik op een wiki-/Markdown-link om hem te openen (`Ctrl`+klik voor een nieuw tabblad).
- **Rechtsklik** op een selectie of een veld voor Plainva's eigen kopieer-/knip-/plakmenu. `F5` en `Ctrl+R` laden **niet** opnieuw (dat zou tabbladen en niet-opgeslagen buffers verwerpen).
- **Peek-venster:** kop slepen om te verplaatsen, hoekgreep om het formaat te wijzigen; `◀ ▶` is een eigen geschiedenis.
- **Kalender:** klik op de maandnaam om maand/jaar te kiezen; klik op een dag voor zijn dagnotitie.
- **Afbeeldingseditor:** teken met de muis (pen/pijl/rechthoek/tekst/bijsnijden); zoom via de knoppen.

## Zie ook

- [Aan de slag](Getting_Started.md) — interface en editormodi
- [Zoeken](Search.md)
