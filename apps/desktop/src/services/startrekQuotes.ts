// The Star Trek quote catalog moved to @plainva/ui (mobile M3E package D5) so
// the mobile hailing-frequencies sheet shares the exact matcher and data.
// This stub keeps every existing desktop import working unchanged.
export {
  matchStarTrekQuote,
  normalizeQuote,
  STAR_TREK_QUOTES,
  type StarTrekQuote,
} from "@plainva/ui";
