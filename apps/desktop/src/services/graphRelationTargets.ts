/**
 * Moved to `@plainva/ui` (S34) together with `graphActions` — the connect
 * gesture offers the same relation options on both shells, and which options
 * exist is a decision that must not be made twice.
 */
export {
  loadRelationCatalog,
  findRelationOptions,
  removeRelationLink,
  writeRelationLink,
  type RelationOption,
} from "@plainva/ui";
