// The outline parser moved to @plainva/ui (mobile M3E package C) so the mobile
// note context sheet shares the exact heading extraction. Desktop imports stay
// valid through this stub.
export { parseHeadings, slugify, type Heading } from "@plainva/ui";
