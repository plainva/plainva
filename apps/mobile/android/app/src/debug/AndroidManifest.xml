<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only: allow cleartext http:// so the emulator can reach a local
     WebDAV test server on 10.0.2.2. Release builds keep the platform default
     (https only). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:usesCleartextTraffic="true" />
</manifest>
