// The candidate list moved to @plainva/ui beside `resolveRelativeTarget`
// (Sammelplan 2.36, 2026-09-04): one decoder, one grammar for both shells.
// Re-exported so the existing imports and the test keep their path.
export { relativeLinkCandidates } from "@plainva/ui";
